#![allow(unused)]

fn main() {
    // +. -, *, /

    let x: i32 = 1;
    let y: i32 = 2;
    println!("x + y = {}", x + y);
    println!("x - y = {}", x - y);
    println!("x * y = {}", x * y);

    // in rust division of integers rounds down
    println!("x / y = {}", x / y);

    // %  (remainder or mod operation)
    let x: i32 = -1;
    let y: i32 = 2;
    println!("x % y = {}", x % y);

    // literals

    // boolean

    //bitwise
}

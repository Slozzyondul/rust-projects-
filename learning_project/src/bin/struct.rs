#![allow(unused)]

//point struct
struct Point {
    x: i32,
    y: i32,
}

// 3d point struct
struct Point3d {
    x: i32,
    y: i32,
    z: i32,
}

//netsted struct
struct Circle {
    center: Point,
    radius: u32,
}

fn main() {

    // create
    // debug
    // read
    // shortcut
    // copy fields
    // update
}
